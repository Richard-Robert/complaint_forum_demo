import { DisplayStatusPipe } from './display-status.pipe';

describe('DisplayStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
